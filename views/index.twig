<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Consent Form</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
    <style>
        .embed-container {
            width: 100%;
            height: 60vh; /* Adjust height as needed */
            border: none;
        }
        .consent-container {
            margin-top: 20px;
        }

        body{
            font-size: x-large;
        }
    </style>
</head>
<body>
<div class="container consent-container text-center">
    <h1>Consent Form</h1>
    <p class="lead">Please read the consent form below. After reading, confirm your agreement to proceed.</p>
    <form action="/consent" method="post">
        <div class="mb-4" id="pdfContainer">
            <embed class="embed-container" src="/27161 Prolific Consent.pdf" type="application/pdf">
                If this browser does not support PDFs, please download the PDF to view it: 
                <a href="/27161 Prolific Consent.pdf">Download PDF</a>.
            </embed>
        </div>
        <div class="form-check mb-3">
            <input class="form-check-input" type="checkbox" id="consentAgreement" name="consentAgreement" required>
            <label class="form-check-label" for="consentAgreement">
                I have read and agree to the consent form.
            </label>
        </div>
        <div class="form-check mb-4">
            <input class="form-check-input" type="checkbox" id="notNCSUEmployee" name="notNCSUEmployee" required>
            <label class="form-check-label" for="notNCSUEmployee">
                I am not an employee of North Carolina State University.
            </label>
        </div>
        <button type="submit" class="btn btn-primary" id="submitButton">Agree and Continue</button>
    </form>
</div>
<script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>
<script>
    $(document).ready(function() {
        var pdfContainer = document.getElementById('pdfContainer');
        var submitButton = document.getElementById('submitButton');

        pdfContainer.addEventListener('scroll', function() {
            if (pdfContainer.scrollTop + pdfContainer.clientHeight >= pdfContainer.scrollHeight) {
                submitButton.disabled = false;
            }
        });
    });
</script>
</body>
</html>